import React from 'react'

const CharacterDetails = () => {
  /*
  useEffect(() => {
    const fetchDetails = async () => {
      try {
        const response = await axios.get (`https://gateway.marvel.com:443/v1/public/${characterId}`)
        console.log (response)
        setCharacter(response.data)
      } catch (error) {
        console.error(error)
      }
    }

    fetchDetails();
  }, [])
  */
  
  return (
    <div>
      The Hulk
    </div>
  )
}

export default CharacterDetails
